Create an HTML page with basic HTML skeleton
Add React, ReactDOM, Babel and PropTypes as script tags
Add script tag with babel jsx code in it
Create a HelpFrom component that renders an input and a paragraph that displays what user has typed in the form.

Use state to keep track of form value [Optional]